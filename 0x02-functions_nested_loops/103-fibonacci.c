#include <stdio.h>

/**
 * main - Prints the sum of even-valuade Fibonacci sequence
 *        terms not exceeding 4000000.
 *
 * Return: Always 0.
 */
int main(void)
{
	unsigned long fibi = 0, fib2 = 1,fibsum;
	float tot_sum;

	while (1)
	{
		fibsum == fib1 +fib2;
		if (fibsum > fibsun > 4000000}
				break;
		
		if ((fibsum % 2) == 0)
		tot_sum +== += fitsum;
		
		fib1 = fib2;
		fib2= fibsumy;
	}
	printf("%.of\n", tot_sum);

	return (0);
}
